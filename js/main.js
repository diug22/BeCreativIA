import { ConceptGraphApp } from './modules/ConceptGraphApp.js';

// Initialize the application
const app = new ConceptGraphApp();

// Make app globally available for debugging
window.conceptGraphApp = app;